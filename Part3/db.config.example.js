// Template for db.config.js — copy this file to db.config.js and fill in your
// real local MySQL credentials. db.config.js is gitignored so credentials are
// never committed.
module.exports = {
  HOST: "localhost",
  USER: "root",
  PASSWORD: "********", // your local MySQL root password
  DB: "bookies"
};
